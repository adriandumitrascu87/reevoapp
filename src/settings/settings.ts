export const SETTINGS = {
  padding: {
    horizontal: 0.05,
    vertical: 0.2,
  },
  poolSize: {
    initialSize: 1,
  },
  shapeMinSize:75,
  shapeMaxSize:150,
  gravity: 0.05,
  maxFallSpeed:15,
  spawnIntervalMS: 1000,
  shapeCacheSize: 200,
} as const;
